push:
    - match:
        owner: "dataxu"
        branch: "dev-.*"
        no_branch: "dev-(auto_pom_update|auto_bump_snaps)"
      trigger:
        job: "{repo}_dev-feature"
        params:
            branch: "{branch}"
